// URL del backend (jobsradar-api). Repos separados, orígenes distintos — ver
// ARCHITECTURE.md AD-11 y sección 7.1 (CORS).

export const API_BASE_URL = import.meta.env.VITE_API_URL ?? "http://localhost:3000";
