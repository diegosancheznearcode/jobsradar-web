packages:
  - "."
allowBuilds:
  msw: true
minimumReleaseAgeExclude:
  # Only 0.5.0 exists under this scope: publish-contracts only ever publishes
  # packages/contracts' current version, never republishes prior ones, so the
  # 0.1.0-0.4.0 history under the old @diegosancheznearcode scope has no
  # equivalent here.
  - '@nearcodecr/jobsradar-contracts@0.5.0'
